{
  "name": "@velocity/core",
  "private": true,
  "version": "0.0.0",
  "description": "Shared domain types. See /docs/architecture/domain-model.md — these types are the compiled mirror of that document.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "echo \"@velocity/core: no tests yet — business logic arrives with later steps\""
  }
}
