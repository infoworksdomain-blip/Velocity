{
  "name": "@velocity/providers",
  "private": true,
  "version": "0.0.0",
  "description": "VideoProvider/ImageProvider/TTSProvider/TranscriptionProvider interfaces and the capability-based router (ADR 0004, STEP 8). Adapters are deterministic stubs pending funded vendor credentials (Kling, Veo, Seedance, MiniMax, Seedream, ElevenLabs, WhisperX) — see docs/steps/STEP-08.md.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "playwright": "1.63.0",
    "zod": "^3.24.1",
    "@velocity/contracts": "workspace:*",
    "@velocity/text-engine": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.4.3",
    "vitest": "^3.2.6"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  }
}
