{
  "name": "@velocity/ui",
  "private": true,
  "version": "0.0.0",
  "description": "Token-driven design-system component library (Appendix A). No pre-built dist — CSS Modules need a consuming bundler (Next's webpack via transpilePackages, or Storybook's Vite) to process them, so this package ships TypeScript/CSS source directly.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "clsx": "^2.1.1"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@storybook/react": "8.4.7",
    "@storybook/react-vite": "8.4.7",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "4.3.4",
    "storybook": "8.4.7",
    "typescript": "^5.7.2",
    "vite": "5.4.11"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "echo \"@velocity/ui: visual correctness is Storybook's job; no separate test runner yet\"",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build",
    "audit-tokens": "node scripts/audit-tokens.mjs",
    "check-story-coverage": "node scripts/check-story-coverage.mjs"
  }
}
