{
  "name": "@velocity/text-engine",
  "private": true,
  "version": "0.0.0",
  "description": "TextProvider abstraction and the TextPlan contract (STEP 8B, ADR 0005). Real Anthropic (forced tool-use) and OpenAI (strict JSON schema) adapters, prompt construction, validation/repair loop, and the layout engine (auto-fit, safe areas, legibility) that packages/render's text overlay components consume.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "zod": "^3.24.1",
    "zod-to-json-schema": "^3.24.1",
    "@anthropic-ai/sdk": "^0.32.1",
    "openai": "^4.77.0",
    "@velocity/contracts": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.4.3",
    "vitest": "^3.2.6",
    "@types/node": "^22.10.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  }
}
